
{{ form_start(form) }}

<div class="row">
    <div class="col s2">
        {{ form_row(form.produit) }}
    </div>
    <div class="col s1">
        {{ form_row(form.quantite) }}
    </div>
    <div class="col s1 etat">
         {{ form_row(form.etat) }}
    </div>
    <div class="col s1">
        {{ form_row(form.submit) }}
    </div>

</div>

{{ form_end(form) }}