{{ form_start(form) }}

<div class="row">

    <div class="col s2">
        {{ form_row(form.num_secu) }}
    </div>

    <div class="col s2">
        {{ form_row(form.nom) }}
    </div>

    <div class="col s1">
        {{ form_row(form.date_naissance) }}
    </div>

    <div class="col s1">
        {{ form_row(form.num_tel) }}
    </div>

    <div class="col s1">
        {{ form_row(form.sexe) }}
    </div>

    {#<div class="col s1">
        {{ form_row(form.date_ajout) }}
    </div>#}

    <div class="col s1">
        {{ form_row(form.statut) }}
    </div>

    <div class="right center submit">
        {{ form_widget(form.submit) }}
    </div>

</div>

{{ form_end(form) }}